{
  "name": "@echohello/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build:electron": "tsc -p tsconfig.build.json",
    "dev": "bun run build:electron && electron .",
    "build:web-assets": "echo \"(placeholder) bundle @echohello/web first\"",
    "build": "bun run build:electron && tsc -p tsconfig.typecheck.json --noEmit",
    "typecheck": "tsc -p tsconfig.typecheck.json --noEmit",
    "lint": "oxlint src --config ../../oxlint.jsonc"
  },
  "dependencies": {
    "@echohello/protocol": "workspace:*",
    "@echohello/server": "workspace:*",
    "electron-log": "^5.4.3"
  },
  "devDependencies": {
    "electron": "^41.0.0",
    "typescript": "^5.9.3"
  }
}
